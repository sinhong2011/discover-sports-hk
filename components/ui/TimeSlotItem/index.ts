/**
 * TimeSlotItem component exports
 * Extracted from home folder to be shared across the app
 */

// Re-export types from time.ts for convenience
export type { AvailabilityLevel, TimeSlotData } from '@/types/time';
export { default, TimeSlotItem } from './TimeSlotItem';
export type { TimeSlotItemProps } from './types';
